import { CompanyListDirective } from './company-list.directive';

describe('CompanyListDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanyListDirective();
    expect(directive).toBeTruthy();
  });
});
